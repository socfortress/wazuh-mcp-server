# Example Wazuh Clusters Configuration

clusters:
  # Production cluster
  prod:
    name: prod
    api_url: https://wazuh.company.tld:55000
    username: api-user
    password: S3cr3t!
    ssl_verify: true

  # Lab/testing cluster
  lab:
    name: lab
    api_url: https://wazuh-lab.company.tld:55000
    username: wazuh
    password: lab123
    ssl_verify: false  # For self-signed certificates in testing

  # Staging cluster
  staging:
    name: staging
    api_url: https://wazuh-staging.company.tld:55000
    username: staging-user
    password: staging-pass
    ssl_verify: true

  # Development cluster
  dev:
    name: dev
    api_url: https://localhost:55000
    username: wazuh-user
    password: wazuh-pass
    ssl_verify: false  # For local development
